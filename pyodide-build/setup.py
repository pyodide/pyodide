# Needed for editable install
import setuptools

if __name__ == "__main__":
    setuptools.setup(
        # entry_points = {
        #     "console_scripts" :
        #     ["foo = foomod:main"]
        # }
    )
