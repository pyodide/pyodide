package:
  name: cpp-exceptions-test
  version: "0.1"
source:
  path: src
build:
  sharedlibrary: true
  script: |
    em++ -c main.cpp -o main.o -fPIC -fexceptions -O2
    mkdir dist
    em++ main.o -sSIDE_MODULE -o dist/cpp-exceptions-test.so -fexceptions
