[project]
name = "test-dummy-unNormalized"
authors = [
  { name="Pyodide"},
]
description = "Just a dummy package for testing Pyodide package loading logics"
version = "1.0.0"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.sdist]
ignore-vcs = true

[tool.hatch.build.targets.wheel]
ignore-vcs = true
include = [
  "dummy_unnormalized/",
]
