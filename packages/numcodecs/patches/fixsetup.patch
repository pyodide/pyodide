diff --git a/setup.py b/setup.py
index 3cf27db..74bdea5 100644
--- a/setup.py
+++ b/setup.py
@@ -6,6 +6,7 @@ import sys
 from distutils.command.build_ext import build_ext
 from distutils.errors import CCompilerError, DistutilsExecError, \
     DistutilsPlatformError
+from cmake_build_ext import cmake_build_ext
 
 try:
     from Cython.Build import cythonize
@@ -292,7 +293,7 @@ def run_setup(with_extensions):
     if with_extensions:
         ext_modules = (blosc_extension() + zstd_extension() + lz4_extension() +
                        compat_extension() + vlen_extension())
-        cmdclass = dict(build_ext=ve_build_ext)
+        cmdclass = dict(build_ext=cmake_build_ext)
     else:
         ext_modules = []
         cmdclass = dict()
