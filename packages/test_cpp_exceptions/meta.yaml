package:
  name: test_cpp_exceptions
  version: "0.1"
source:
  path: src
build:
  sharedlibrary: true
  script: |
    em++ -c main.cpp -o main.o -fPIC -fexceptions -O2
    mkdir dist
    em++ main.o -sSIDE_MODULE -o dist/test_cpp_exceptions.so -fexceptions

