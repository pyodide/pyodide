package:
  name: pyio
  version: 1.0.0 # Nonsense
  tag:
    - always
  top-level:
    - _pyio
build:
  type: cpython_module
  script: |
    mkdir dist
    export DISTDIR=$(pwd)/dist
    cd $CPYTHONBUILD

    cp Lib/_pyio.py $DISTDIR
